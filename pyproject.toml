[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "balapan-chicken-bot"
version = "1.0.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "aiogram==2.25.1",
]